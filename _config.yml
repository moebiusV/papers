# GitHub Pages / Jekyll configuration
permalink: pretty

# Only process content directories — skip build artifacts and tooling
exclude:
  - build
  - private
  - Makefile
  - COPYING
  - AUTHORS
  - NEWS
  - ChangeLog
  - INSTALL
